# 生產環境 - 完整模式（docker compose -f docker-compose.yml -f docker-compose.prod-full.yml up -d）
services:
  backend:
    build:
      additional_contexts:
        src: ${BACKEND_PATH}
    environment:
      NODE_ENV: production

  frontend:
    build:
      additional_contexts:
        src: ${FRONTEND_PATH}
      target: runner-full
    environment:
      NODE_ENV: production
